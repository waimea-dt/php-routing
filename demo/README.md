# A Demo of a PHP Routing System with HTMX

This demo shows off various uses of the routing system:

- POSTing form data
- Login / out via PHP sessions
- HTMX swapping of UI elements (e.g. for editing)
- Etc.

